# EasyUp parsers

### First install project

**First run:**

docker-compose up --build -d

docker exec -it php_kinopoisk composer install

**First run:**

docker-compose up -d

**Stop docker:**

docker-compose stop